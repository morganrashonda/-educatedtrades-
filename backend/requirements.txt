# Educated Trades — backend dependencies. Pinned for reproducible builds.
#
# Only what the code actually imports. The previous list pinned pandas, scipy,
# Flask, flask-cors, finnhub-python, websocket-client, websockets and
# python-dotenv — none of which appear in any import statement. scipy alone
# requires Python 3.10+, so an unused dependency was breaking installation on
# 3.9 for no benefit.

numpy==1.26.4           # patterns.py — indicator maths
alpaca-py==0.43.0       # trading.py, main.py — broker client
pandas==2.3.3           # alpaca-py compatibility — pandas 3 drops pytz
pytz==2026.3.post1      # alpaca.data imports this directly during backfill
requests==2.32.3        # news_ingestion.py — Finnhub REST calls
vaderSentiment==3.3.2   # sentiment.py — retained for news telemetry only;
                        # sentiment no longer contributes to the signal path

# Testing. The two primary suites (test_suite.py, test_end_to_end.py) are
# stdlib-only and need nothing installed; pytest is for the older tests.
pytest>=7.4.0
