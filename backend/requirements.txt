# Educated Trades — Backend Dependencies
# Generated 2026-07-04. Pin everything for reproducible builds.

numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
requests==2.32.3
Flask==3.1.0
flask-cors==5.0.0
alpaca-py==0.43.0
finnhub-python==2.4.29
# Sentiment scoring. Without this the engine silently degrades to keyword-only
# matching -- it still runs, but VADER contributes 0.0 to every conviction.
vaderSentiment==3.3.2
websocket-client==1.8.0
websockets==12.0
python-dotenv==1.0.1

# Testing
pytest>=7.4.0
